<?php


/*
*Determines if the argument is even or odd
*/

function isEven($n) // parameter
{
    if ($n%2==0) {
        return true;
    } 
    return false; //else {return false;}  if we wite this code then it wouldn't be execute because when the condition came true then it return and can't check other things   

}